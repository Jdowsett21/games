import React from 'react';
import CardGrid from '../components/common/CardGrid';

function Puzzles({ location }) {
  return (
    <div>
      <CardGrid location={location} />
    </div>
  );
}

export default Puzzles;
